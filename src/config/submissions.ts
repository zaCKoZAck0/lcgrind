// Shared between the submission form (client) and the server action; keep
// this module free of server-only imports.
export const WEEKLY_SUBMISSION_CAP = 7;
export const MIN_TEXT_LENGTH = 80;
export const MAX_TEXT_LENGTH = 10000;
